# No theme: dinky's stylesheet is vendored into _sass/dinky-base.scss and its
# layout was never used - _layouts/default.html has always been ours. Keeping
# it as a gem meant a second set of breakpoints we could not see or edit.
release_version: "0.10"
google_analytics: "G-6MBRPLS92C"

# Internal documentation that lives on this branch so intake branches can
# reach it, but must not be served from hymnsinger.com. The files also
# carry `published: false`; this list is the belt to that pair of braces.
exclude:
  - song-intake-protocol.md
  - agent-prompt-song-intake.md
